[project]
name = "random_walk_kernels_computations"
version = "0.1.0" 
requires-python = ">=3.11"
dependencies = [
    "ipykernel",
    "numpy",
    "scipy",
    "networkx",
    "torch",
    "torch_geometric",
    "scikit-learn",
    "matplotlib"
]